# ARCHITECTURE.md
# المعمارية التقنية لنظام الذاتية والأرشفة
## مركز الدراسات الإفريقية

> **حالة الوثيقة:** هذه الوثيقة تصف **المعمارية الحالية** (نموذج أولي محلي: React + localStorage، بلا Backend). المعمارية **المستهدفة** (Backend + PostgreSQL + تخزين ملفات مركزي + سجلات تدقيق + نسخ احتياطي) موثقة في `DEVELOPMENT_PLAN.md` (المرجع المعتمد لمراحل التطوير)، ولا تُنفَّذ إلا عبر مراحله.
> حُدِّثت ضمن **PHASE 0** (تدقيق معماري وتوثيقي فقط) لتطابق الكود المصدري الفعلي.

يتبع النظام معمارية معيارية متعددة الطبقات (Clean Modular Architecture) تفصل بين نماذج البيانات، والخدمات، والواجهات البرمجية، والمكونات البصرية.

---

## المخطط المعماري العام (System Architecture Overview)

```text
┌───────────────────────────────────────────────────────────┐
│                    Presentation Layer                     │
│  ┌──────────────────┐ ┌─────────────────┐ ┌────────────┐  │
│  │   Layout View    │ │ Primary Views   │ │   Modals   │  │
│  │     (Header)     │ │ (List/Reports)  │ │ (LightBox) │  │
│  └────────┬─────────┘ └────────┬────────┘ └─────┬──────┘  │
└───────────┼────────────────────┼────────────────┼─────────┘
            │                    │                │
┌───────────▼────────────────────▼────────────────▼─────────┐
│                     Services Layer                        │
│   ┌──────────────────────────┐ ┌──────────────────────┐   │
│   │      StorageService      │ │  StatisticsService   │   │
│   │ (Persistence & Backups)  │ │  (Metrics & Reports) │   │
│   └──────────────────────────┘ └──────────────────────┘   │
│           ┌──────────────────────────────────────┐        │
│           │            AuthService               │        │
│           │ (Mock Users, Permissions, Filtering) │        │
│           └──────────────────────────────────────┘        │
└───────────────────────────┬───────────────────────────────┘
                            │
┌───────────────────────────▼───────────────────────────────┐
│                   Domain & Core Models                    │
│   ┌──────────────────────────┐ ┌──────────────────────┐   │
│   │    src/core/models/      │ │ src/core/interfaces/ │   │
│   │  (Transaction, Employee, │ │   (Storage Contracts │   │
│   │      DailySituation)     │ │     & Interfaces)    │   │
│   └──────────────────────────┘ └──────────────────────┘   │
└───────────────────────────────────────────────────────────┘
```

---

## 1. طبقة النماذج المركزية (`src/core/`)
تحتوي على التعريفات الصرفة للمجال الإداري (Domain Models) بدون أي تبعية للمكتبات الخارجية أو عناصر React:
- **`models/transaction.ts`**: نموذج المعاملة (العدد، التاريخ، التوجيه، الهامش، الأولوية، المرفقات، الحالة).
- **`models/employee.ts`**: نموذج المنتسب والباحث (اللقب، الدرجة الأكاديمية، الشعبة، الفئة).
- **`models/dailySituation.ts`**: نموذج الموقف اليومي الرسمي لكافة فئات العاملين (يُخزن حالياً مدمجاً داخل المعاملة عبر `isDailySituation` و`dailySituationData`).
- **`models/permission.ts`**: نظام الصلاحيات المبني على الأدوار (RBAC) — 22 صلاحية و4 أدوار (`admin`، `director`، `archivist`، `employee`) مع مصفوفة `ROLE_PERMISSIONS` ودوال الفحص النقية.
- **`models/user.ts`**: نموذج المستخدم النشط (مع `employeeId` و`customPermissions`) ودوال فحص صلاحيات المستخدم.
- **`models/accessScope.ts`**: نطاقات الرؤية الأربعة (`PublicToEmployees`, `SpecificEmployees`, `Administrative`, `DirectorOnly`) + الواجهة الوسيطة `TransactionEmployeeRelation` + الدالة النقية `canUserAccessTransaction` (قابلة للنقل مستقبلاً إلى الخادم).
- **`models/navigation.ts`**: معايير التصفح والتنقل وتحديد الأدوار.
- **`interfaces/storage.ts`**: واجهات وعقود التخزين والنسخ الاحتياطي (انظر الملاحظة في القسم 2).

---

## 2. طبقة الخدمات والمساعدات (`src/services/` & `src/utils/`)
- **`StorageService`**: عزل عمليات القراءة والحفظ والتأكد من عدم تلف البيانات مع إدارة النسخ الاحتياطية.
- **`StatisticsService`**: حساب نسب الإنجاز، مؤشرات الكتب الصادرة والواردة، ومعدلات الأداء الشهرية.
- **`employeeUtils`**: استخراج وتحليل أسماء الكوادر وفصلها ومطابقتها بذكاء وفك الارتباط.
- **`attachmentUtils`**: معالجة رفع الملفات والمستندات المصورة (ضغط Canvas حتى 1600px بجودة 0.82) وتخزينها كـ Base64 data URLs، وضمان أمان المعاينة والتكبير.
- **`authService`**: توثيق وهمي حالياً — اشتقاق المستخدم من `mockUsers.ts` حسب الدور (4 أدوار)، وتصفية المعاملات حسب نطاق الرؤية `canUserAccessTransaction`، وتطبيع المعاملات (`normalizeTransaction`: ربط `employeeIds`، مزامنة `employeeName`، تعيين `visibility` الافتراضي).

> **ملاحظة معمارية:** العقد `IDataStorage` في `core/interfaces/storage.ts` معلَن لكنه **غير مُنفَّذ** فعلياً بواسطة `StorageService` (اختلاف التواقيع: `exportBackup`/`restoreFromBackup` مقابل `exportBackupJson`/`restoreFromBackupJson`). يُوثَّق هذا كمسألة غير محسومة في `DEVELOPMENT_PLAN.md` ولا يُصحَّح في إطار PHASE 0.

### 2.1 طبقة البيانات الأولية (`src/data/`)
- **`mockData.ts`**: 8 منتسبين تجريبيين (`emp-1` إلى `emp-8`) و7 معاملات ابتدائية (بما فيها استمارة موقف يومي واحد).
- **`mockUsers.ts`**: 4 مستخدمين تجريبيين معزولين (`director`, `archivist`, `employee`, `admin`) لاختبار الأدوار ونطاقات الرؤية.

---

## 3. طبقة الواجهات والمكونات (`src/components/`)
مقسّمة وفق مبدأ المسؤولية المحددة:
- **`layout/`**:
  - `Header.tsx`: الترويسة الرئيسية، زر التبديل السريع بين دور المدير ومسؤول الذاتية، التبديل بين الوضع الليلي والنهاري، وشارة الإشعارات للكتب غير المقروءة.
- **`views/`**:
  - `TransactionsList.tsx`: سجل المعاملات والكتب الرسمية مع الفلترة المتقدمة والفرز ومحرك البحث.
  - `MonthlyReportView.tsx`: التقرير الشهري التراكمي الشامل مع استعراض الوثائق والمؤشرات.
  - `EmployeesView.tsx`: سجل الكوادر والباحثين مع تاريخ المعاملات وإمكانية التحرير وفك الارتباط.
  - `DailySituationsView.tsx`: جدول استعراض وتدقيق استمارات المواقف اليومية المعتمدة.
  - `ArchivistStudioView.tsx`: استوديو الأرشفة التفاعلي للمسؤول لتعديل التسلسلات والمرفقات.
- **`modals/`**:
  - `TransactionDetailModal.tsx`: بطاقة تفاصيل المعاملة الشاملة مع عارض الوثائق المصورة والهوامش.
  - `NewTransactionModal.tsx`: استمارة إدراج كتاب أو معاملة جديدة مع السحب والإفلات للمرفقات.
  - `DailySituationDocumentModal.tsx`: الاستمارة الرسمية للموقف اليومي جاهزة للطباعة والتصدير.
  - `ImageLightboxModal.tsx`: عارض الوثائق المكبر عالي الدقة مع أدوات التدوير والتحريك.
  - `ArchivistEditorModal.tsx`: نافذة تحرير ومراجعة الأرشفة للمسؤول.

---

## 4. نموذج تدفق البيانات وإدارة الحالة
تتم إدارة الحالة العامة مركزياً في `App.tsx` مع حقن البيانات والـ callbacks للواجهات الفرعية عبر Props واضحة، مع مزامنة فورية وآمنة للتخزين المحلي (`localStorage`) لضمان عدم ضياع أي تغيير أثناء العمل اليومي.

### 4.1 تدفق الأدوار والصلاحيات (كما هو حالياً)
1. الدور النشط (`userRole`) يُحدد من الترويسة (`Header.tsx`) أو من معامل الرابط `?role=director|archivist`.
2. `AuthService.getUserForRole` يشتق كائن `User` من `mockUsers.ts`.
3. `AuthService.filterTransactionsForUser` يفلتر المعاملات عبر `canUserAccessTransaction` (نطاق الرؤية + الصلاحيات) قبل العرض.
4. عناصر الواجهة (مثل قسم تحرير الذاتية) تظهر/تختفي وفق `roleHasPermission` — **الفرض كله من جهة العميل** (توثيق وهمي)، والانتقال إلى الفرض على الخادم موثق في `DEVELOPMENT_PLAN.md` (PHASE 14–16).
