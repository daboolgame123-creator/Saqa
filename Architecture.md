# المعمارية التقنية لنظام الذاتية والأرشفة والتوثيق الإداري

## مركز الدراسات الإفريقية

> **حالة الوثيقة:**
> هذه الوثيقة تصف **المعمارية الحالية الفعلية** للمشروع بعد تنفيذ PHASE 0.
> الوضع الحالي هو نموذج أولي محلي يعتمد على React + localStorage ولا يحتوي على Backend أو قاعدة بيانات مركزية.
>
> أما المعمارية المستهدفة ومراحل الانتقال من النموذج الأولي إلى النظام المركزي، فتُحدد وفق `ALSQAYA_PLAN.md`.
>
> وثائق المراحل القديمة التي تم تجاوزها موجودة في `docs/archive/` ولا تمثل الخطة الحالية.

يتبع النظام حاليًا بنية معيارية متعددة الطبقات (Clean Modular Architecture) تفصل بين نماذج المجال، والخدمات، وبيانات الاختبار، ومكونات الواجهة.

---

## المخطط المعماري العام — الوضع الحالي

```text
┌───────────────────────────────────────────────────────────┐
│                    Presentation Layer                     │
│                                                           │
│  ┌──────────────────┐ ┌─────────────────┐ ┌────────────┐ │
│  │   Layout View    │ │ Primary Views   │ │   Modals   │ │
│  │     (Header)     │ │ (List/Reports)  │ │ (LightBox) │ │
│  └────────┬─────────┘ └────────┬────────┘ └─────┬──────┘ │
└───────────┼────────────────────┼────────────────┼────────┘
            │                    │                │
┌───────────▼────────────────────▼────────────────▼────────┐
│                     Services Layer                       │
│                                                           │
│  ┌──────────────────────────┐ ┌────────────────────────┐ │
│  │      StorageService      │ │  StatisticsService     │ │
│  │ (Local Persistence &     │ │  (Metrics & Reports)   │ │
│  │  Backup)                 │ │                        │ │
│  └──────────────────────────┘ └────────────────────────┘ │
│                                                           │
│  ┌──────────────────────────────────────────────────────┐ │
│  │                    AuthService                       │ │
│  │        (Mock Authentication / Authorization)         │ │
│  └──────────────────────────────────────────────────────┘ │
└───────────────────────────┬───────────────────────────────┘
                            │
┌───────────────────────────▼───────────────────────────────┐
│                   Domain & Core Models                     │
│                                                           │
│  ┌──────────────────────────┐ ┌────────────────────────┐ │
│  │    src/core/models/      │ │ src/core/interfaces/   │ │
│  │                          │ │                        │ │
│  │ Transaction, Employee,  │ │ Storage Contracts       │ │
│  │ DailySituation, User,   │ │ & Interfaces            │ │
│  │ Permission, AccessScope  │ │                        │ │
│  └──────────────────────────┘ └────────────────────────┘ │
└───────────────────────────────────────────────────────────┘
```

> **مهم:** هذا المخطط يصف الوضع الحالي فقط.
> لا يمثل هذا المخطط المعمارية النهائية للنظام المركزي.

---

# 1. طبقة النماذج المركزية (`src/core/`)

تحتوي على تعريفات المجال الإداري (Domain Models) دون اعتماد مباشر على React أو تفاصيل واجهة المستخدم.

## النماذج الحالية

### `models/transaction.ts`

يمثل المعاملة/الكتاب في النموذج الأولي، ويتضمن البيانات الموجودة حاليًا مثل:

* العدد.
* التاريخ.
* التوجيه.
* الهامش.
* الأولوية.
* المرفقات.
* الحالة.
* بيانات مرتبطة بالموقف اليومي في النموذج الحالي.

> النموذج الحالي لا يمثل بالضرورة الشكل النهائي لكيان Transaction بعد الانتقال إلى Backend وقاعدة البيانات.

### `models/employee.ts`

يمثل المنتسب والباحث والبيانات الأساسية المرتبطة به في النموذج الحالي، مثل:

* الاسم.
* اللقب.
* الدرجة الأكاديمية.
* الشعبة.
* الفئة.

### `models/dailySituation.ts`

يمثل نموذج الموقف اليومي.

في النموذج الأولي الحالي، توجد بيانات الموقف اليومي مدمجة مع المعاملة من خلال:

* `isDailySituation`
* `dailySituationData`

وهذا **وصف للحالة الحالية فقط**.

وفق الخطة المعتمدة، سيتم التعامل مع الموقف اليومي ككيان مستقل في مرحلة:

**Phase 6 — Daily Situation**

وذلك دون افتراض تفاصيل أعمال غير محسومة في الخطة.

### `models/permission.ts`

يحتوي حاليًا على نظام صلاحيات مبني على الأدوار (RBAC) للاستخدام في النموذج الأولي، بما يشمل:

* الأدوار الحالية.
* الصلاحيات.
* `ROLE_PERMISSIONS`
* دوال فحص الصلاحيات النقية.

الأدوار الحالية:

* `admin`
* `director`
* `archivist`
* `employee`

> وجود RBAC في `core` حاليًا لا يعني أن فرض الصلاحيات النهائي يتم على العميل. في النظام النهائي يجب فرض الصلاحيات على الخادم.

### `models/user.ts`

يمثل المستخدم النشط في النموذج الأولي، ويحتوي على معلومات مثل:

* `employeeId`
* `customPermissions`

مع دوال فحص الصلاحيات المستخدمة حاليًا من الواجهة.

### `models/accessScope.ts`

يمثل نطاقات الرؤية الحالية:

* `PublicToEmployees`
* `SpecificEmployees`
* `Administrative`
* `DirectorOnly`

ويحتوي أيضًا على العلاقات الحالية المرتبطة بالمعاملات، مثل:

* `TransactionEmployeeRelation`
* `canUserAccessTransaction`

هذه الدوال مستخدمة حاليًا على جانب العميل، وسيتم نقل قرار الوصول الفعلي إلى Backend عند تنفيذ مراحل:

* **Phase 11 — RBAC / Permissions**
* **Phase 12 — Access Scope**

### `models/navigation.ts`

يحتوي على معايير التصفح والتنقل المرتبطة بالأدوار في النموذج الأولي.

### `interfaces/storage.ts`

يحتوي على العقود والواجهات الخاصة بالتخزين والعمليات المرتبطة به.

---

# 2. طبقة الخدمات والمساعدات (`src/services/` و `src/utils/`)

## `StorageService`

يعزل عمليات:

* القراءة.
* الحفظ.
* التحقق من سلامة البيانات.
* التخزين المحلي.
* النسخ الاحتياطية في النموذج الأولي.

هذه المسؤوليات مرتبطة بالبنية الحالية، وليست تمثيلًا لنظام التخزين النهائي.

عند الانتقال إلى Backend وقاعدة البيانات، سيتم استبدال/إعادة توجيه هذه المسؤوليات وفق المعمارية المستهدفة في `ALSQAYA_PLAN.md`.

## `StatisticsService`

مسؤول عن الحسابات والإحصاءات المستخدمة حاليًا، مثل:

* نسب الإنجاز.
* مؤشرات الكتب الواردة والصادرة.
* المعدلات الشهرية.

## `employeeUtils`

يحتوي على وظائف مساعدة مرتبطة ببيانات المنتسبين، مثل:

* استخراج وتحليل الأسماء.
* الفصل والمطابقة.
* فك الارتباط.

## `attachmentUtils`

يعالج المرفقات في النموذج الأولي الحالي، بما في ذلك:

* معالجة الصور.
* الضغط.
* المعاينة.
* التكبير.
* التحويل إلى Base64 data URLs.

> **ملاحظة مهمة:** تخزين المرفقات بصيغة Base64 داخل LocalStorage هو جزء من النموذج الأولي الحالي فقط، وليس التصميم النهائي.
> التصميم النهائي يعتمد على تخزين الملفات خارج قاعدة البيانات مع حفظ بياناتها الوصفية في قاعدة البيانات، وفق `ALSQAYA_PLAN.md`.

## `authService`

يقدم حاليًا توثيقًا وهميًا للنموذج الأولي، ويعتمد على:

* `mockUsers.ts`
* الأدوار التجريبية.
* الصلاحيات الموجودة في `core`.
* `canUserAccessTransaction`

ويستخدم لتحديد المستخدم التجريبي وتصفية البيانات قبل عرضها.

> **حد أمني:** `authService` الحالي ليس آلية أمان حقيقية؛ فهو يعمل بالكامل على جانب العميل.
>
> عند تنفيذ Backend، تصبح المصادقة والتفويض الفعليان مسؤولية الخادم وفق المراحل:
>
> * Phase 10 — Authentication
> * Phase 11 — RBAC / Permissions
> * Phase 12 — Access Scope

---

# 2.1 طبقة البيانات الأولية (`src/data/`)

تحتوي على بيانات الاختبار المستخدمة في النموذج الأولي.

## `mockData.ts`

تحتوي حاليًا على:

* 8 منتسبين تجريبيين.
* 7 معاملات ابتدائية.
* استمارة موقف يومي ضمن البيانات التجريبية.

هذه البيانات ليست مصدر البيانات النهائي للنظام.

## `mockUsers.ts`

تحتوي على 4 مستخدمين تجريبيين لاختبار:

* الأدوار.
* الصلاحيات.
* نطاقات الرؤية.

هذه البيانات تستخدم للاختبار فقط.

---

# 3. طبقة الواجهات والمكونات (`src/components/`)

مقسمة وفق مبدأ المسؤولية المحددة.

## `layout/`

### `Header.tsx`

الترويسة الرئيسية، وتشمل في النموذج الحالي:

* التنقل.
* التبديل بين الوضع الليلي والنهاري.
* شارة الإشعارات.
* أدوات مرتبطة بالمستخدم/الدور التجريبي.

> بعض أدوات تبديل الأدوار الحالية مخصصة للنموذج الأولي والاختبار، وليست آلية مصادقة نهائية.

## `views/`

### `TransactionsList.tsx`

سجل المعاملات والكتب الرسمية مع:

* الفلترة.
* الفرز.
* البحث.

### `MonthlyReportView.tsx`

واجهة التقرير الشهري والمؤشرات الحالية.

### `EmployeesView.tsx`

واجهة سجل الكوادر والباحثين والعمليات المتاحة في النموذج الأولي.

### `DailySituationsView.tsx`

واجهة استعراض وتدقيق بيانات الموقف اليومي الحالية.

### `ArchivistStudioView.tsx`

واجهة أدوات الأرشفة الحالية للمسؤول.

## `modals/`

### `TransactionDetailModal.tsx`

عرض تفاصيل المعاملة والمرفقات.

### `NewTransactionModal.tsx`

إدراج كتاب/معاملة جديدة وإضافة المرفقات.

### `DailySituationDocumentModal.tsx`

عرض نموذج الموقف اليومي وتجهيزه للطباعة/التصدير.

### `ImageLightboxModal.tsx`

عرض الوثائق والصور بشكل مكبر.

### `ArchivistEditorModal.tsx`

أدوات تحرير ومراجعة الأرشفة الحالية.

---

# 4. نموذج تدفق البيانات وإدارة الحالة

تتم إدارة الحالة العامة في `App.tsx` في النموذج الأولي الحالي.

يتم تمرير البيانات وعمليات التعديل إلى المكونات الفرعية من خلال Props وCallbacks.

ويتم استخدام `localStorage` كمصدر التخزين الحالي.

> هذه البنية تصف النموذج الأولي فقط.
> عند الانتقال إلى Backend لن تكون `App.tsx` أو `localStorage` مصدر الحقيقة الرئيسي للبيانات.

---

# 4.1 تدفق الأدوار والصلاحيات — الوضع الحالي

في النموذج الأولي الحالي:

1. يتم تحديد الدور النشط (`userRole`) من واجهة الاختبار أو من معامل الرابط.
2. يقوم `AuthService.getUserForRole` باشتقاق المستخدم من `mockUsers.ts`.
3. يقوم `AuthService.filterTransactionsForUser` بتصفية المعاملات باستخدام `canUserAccessTransaction`.
4. تظهر أو تختفي بعض عناصر الواجهة اعتمادًا على `roleHasPermission`.
5. **الفرض الحالي للصلاحيات ونطاقات الرؤية يتم بالكامل على جانب العميل.**

هذا الوضع مقبول فقط باعتباره جزءًا من النموذج الأولي.

---

# 4.2 المعمارية الأمنية المستهدفة

عند الانتقال إلى Backend، يجب ألا تعتمد حماية البيانات أو الصلاحيات على React.

التدفق المستهدف هو:

```text
Identity
   ↓
Authentication
   ↓
User / Role
   ↓
Permission
   ↓
Access Scope
   ↓
Resource
```

ويكون Backend هو المسؤول عن:

* التحقق من هوية المستخدم.
* التحقق من الجلسة/التوكن وفق التصميم المعتمد.
* فرض الصلاحيات.
* تطبيق نطاق الوصول.
* منع الوصول المباشر غير المصرح به إلى البيانات.
* تسجيل الأحداث الأمنية والتغييرات وفق نظام التدقيق.

تفاصيل التنفيذ المرحلي موجودة في:

`ALSQAYA_PLAN.md`

ولا يتم تنفيذ هذه المعمارية في Phase 0.

---

# 5. العلاقة بين المعمارية الحالية والمعمارية المستهدفة

المعمارية الحالية هي نقطة الانطلاق وليست التصميم النهائي.

الانتقال سيكون تدريجيًا، مع الحفاظ على استقرار النظام في كل مرحلة.

المبادئ الأساسية للانتقال:

1. عدم تنفيذ Backend قبل المرحلة المحددة له.
2. عدم تحويل LocalStorage إلى قاعدة بيانات بشكل عشوائي.
3. عدم نقل مسؤوليات إلى Backend قبل تحديد حدودها.
4. الحفاظ على Domain Models قابلة لإعادة الاستخدام.
5. فصل Authentication عن Authorization.
6. فصل RBAC عن Access Scope.
7. عدم اعتبار واجهة المستخدم طبقة أمان.

المراحل المستقبلية وتبعياتها محددة حصريًا في:

`ALSQAYA_PLAN.md`

---

# 6. ملاحظة حول التخزين الحالي

العقد `IDataStorage` في:

`src/core/interfaces/storage.ts`

مطبق فعليًا بواسطة `StorageService`.

يوجد فحص للمطابقة وقت الترجمة في `storageService.ts`، ويشمل العمليات الحالية مثل:

* `loadDarkMode`
* `saveDarkMode`
* مجموعات شؤون المنتسبين.
* النسخ الاحتياطي.

هذا يثبت أن عقد التخزين الحالي متوافق مع تنفيذه في النموذج الأولي.

---

# 7. حدود هذه الوثيقة

هذه الوثيقة لا تحدد:

* قواعد الأعمال التفصيلية.
* ترتيب مراحل التنفيذ.
* حالة إنجاز كل مرحلة.
* تفاصيل المصادقة النهائية.
* تفاصيل RBAC النهائية.
* تفاصيل Access Scope النهائية.
* تصميم قاعدة البيانات النهائي.
* تصميم Backend النهائي.

هذه الأمور، عندما تكون محددة، يجب الرجوع فيها إلى:

`ALSQAYA_PLAN.md`

أما هذه الوثيقة فتبقى المرجع الخاص **بوصف البنية المعمارية الحالية واتجاه الانتقال المعماري**.

---

# 8. قاعدة تحديث الوثيقة

يجب تحديث `ARCHITECTURE.md` عندما يحدث تغيير معماري حقيقي في المشروع.

ولا تستخدم الوثيقة لتسجيل كل تغيير صغير في الكود.

أي تغيير معماري يجب أن يكون:

* مبررًا.
* متوافقًا مع `ALSQAYA_PLAN.md`.
* موثقًا بوضوح.
* منفصلًا عن التغييرات غير المعمارية قدر الإمكان.

---

## حالة الوثيقة

**PHASE 0 — Baseline Architecture Documentation**

الوثيقة تصف المعمارية الفعلية للنموذج الأولي بعد تدقيق Phase 0.

الخطة التنفيذية الحالية والمعتمدة:

`ALSQAYA_PLAN.md`
