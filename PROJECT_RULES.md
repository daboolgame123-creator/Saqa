# PROJECT_RULES — قواعد مشروع السقاية

**حالة الوثيقة:** معتمدة ومزامنة مع `ALSQAYA_PLAN.md`.

## 1. هرمية الوثائق

المرجع التنفيذي الرئيسي للمشروع هو:

`ALSQAYA_PLAN.md`

ويحتوي على:
- قواعد الأعمال المعتمدة.
- القرارات الوظيفية.
- المعمارية التنفيذية ذات الصلة بالمراحل.
- تفاصيل كل مرحلة.
- الاختبارات ومعايير الإغلاق.
- القرارات غير المحسومة التي تحمل `TBD`.

الوثائق التالية لا يجوز أن تناقضه:
- `Architecture.md`
- `PROJECT_VISION.md`
- `README.md`
- `docs/PROJECT_PLAN.md`

تقارير `PHASE_*_REPORT.md` وثائق تاريخية لحالة مرحلة معينة؛ لا تعاد كتابتها لتغيير التاريخ السابق، لكن أي قاعدة مستقبلية متعارضة معها يحكمها `ALSQAYA_PLAN.md`.

## 2. قاعدة عدم الاختراع

Cline ممنوع من اختراع:
- قواعد أعمال.
- حقول غير معتمدة.
- صلاحيات جديدة.
- حالات جديدة.
- علاقات جديدة.
- سلوك UI غير محدد عندما يؤثر في البيانات أو الصلاحيات.

إذا وجد `TBD`:

`TBD ≠ permission to guess`

يبقى القرار غير منفذ حتى يعتمد رسميًا ويُضاف إلى الخطة.

## 3. دورة المرحلة

لكل مرحلة:

1. قراءة المستودع والوثائق.
2. قراءة قسم المرحلة الحالي في `ALSQAYA_PLAN.md` كاملًا.
3. تحديد نطاق التغيير قبل التحرير.
4. تنفيذ المرحلة فقط.
5. تشغيل typecheck/lint/build والاختبارات المناسبة.
6. إصلاح الأخطاء الناتجة عن المرحلة الحالية فقط.
7. مراجعة diff.
8. تحديث الوثائق التي تخص المرحلة عند الحاجة.
9. Commit واضح.
10. التوقف.

لا ينتقل Cline تلقائيًا إلى المرحلة التالية.

## 4. حدود المراحل

- لا تنفيذ لمرحلة مستقبلية داخل المرحلة الحالية.
- لا إعادة تصميم لمجال كامل بسبب تفضيل شخصي.
- لا حذف لسجل تاريخي لأنه غير ظاهر في الواجهة.
- لا نقل للأمان إلى React كبديل عن Backend.

## 5. صحة البيانات

- العلاقات الأساسية تستخدم IDs.
- `employeeName` ليس مفتاح علاقة.
- البيانات التاريخية لا تُحذف لمجرد تغيّر الواجهة.
- أي عملية تغيّر سجلًا مهمًا يجب أن تكون قابلة للتتبع عند وصول مرحلة Audit.
- الاختبارات تستخدم بيانات وهمية منفصلة عن بيانات العمل.

## 6. الأمن

المصادقة تثبت الهوية.

RBAC يحدد ما يستطيع المستخدم فعله.

Access Scope يحدد ما يستطيع رؤيته.

القرار النهائي:

`Identity → Permission → Access Scope → Resource`

والتحقق النهائي من الصلاحيات على الخادم.

## 7. التصميم البرمجي

نحافظ على فصل المسؤوليات:

- UI للعرض والتفاعل.
- Services لقواعد المجال.
- Controllers/Routes لواجهة API.
- Repositories للوصول للبيانات.
- Database لمصدر الحقيقة بعد Phase 9.
- Background Jobs للعمليات الثقيلة أو الزمنية عند المرحلة التي تعتمدها.

لا يوضع منطق أعمال ثقيل داخل React components.

## 8. الاختبارات

الحد الأدنى عند اكتمال أي مرحلة:
- TypeScript/typecheck.
- Lint عند توفره.
- Build.
- Tests الخاصة بالمرحلة.
- Regression مناسب.

وجود baseline قديم لا يبرر إضافة أخطاء جديدة.

## 9. Git

- لا `force push`.
- لا حذف لنقطة استقرار سليمة.
- كل مرحلة لها commit واضح.
- الفروع المرحلية تستخدم عند الحاجة.
- لا يعتمد اكتمال المرحلة على رسالة Cline وحدها؛ يعتمد على الاختبارات والمراجعة والـdiff.

## 10. التغيير في القواعد

إذا ظهرت قاعدة جديدة أثناء التنفيذ:

`اكتشاف → توثيق → اعتماد → تحديث ALSQAYA_PLAN → تنفيذ في المرحلة المناسبة`

ولا تُطبق مباشرة داخل الكود قبل توثيقها.
