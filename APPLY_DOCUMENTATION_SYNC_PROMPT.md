# Cline — Apply Final ALSQAYA Documentation Synchronization

## المهمة
طبّق مزامنة وثائق السقاية فقط. لا تغيّر كود التطبيق ولا ملفات `src/` ولا `package.json` ولا قاعدة البيانات.

## المصدر المعتمد
الملفات الموجودة في مجلد `ALSQAYA_DOCUMENTATION_SYNC` هي النسخ النهائية المعتمدة.
استبدل الملفات التالية داخل المستودع بنسخها المطابقة:

- `ALSQAYA_PLAN.md`
- `Architecture.md`
- `PROJECT_RULES.md`
- `PROJECT_VISION.md`
- `README.md`
- `docs/PROJECT_PLAN.md`
- `docs/DOCUMENTATION_SYNC_REPORT.md` (ملف جديد)

## القواعد
1. اقرأ الملفات الحالية أولًا، ثم اقرأ النسخ الجديدة.
2. لا تعدّل أي ملف خارج القائمة.
3. لا تحذف أو تعيد كتابة تقارير `PHASE_0_REPORT.md` أو `PHASE_6_REPORT.md` أو `PHASE_7_REPORT.md`؛ هي تاريخ التنفيذ.
4. لا تغيّر أي كود.
5. لا تنفذ Phase 8 أو أي مرحلة برمجية.
6. لا تضف قواعد أعمال جديدة.
7. `ALSQAYA_PLAN.md` هو المرجع التنفيذي الرئيسي بعد المزامنة.

## التحقق
بعد الاستبدال:
- `git diff --stat`
- `git diff --check`
- تحقق أن جميع الملفات السبعة موجودة.
- تحقق أن `ALSQAYA_PLAN.md` يقول إن Phase 0–7 مكتملة وأن Phase 8 هي التالية.
- تحقق من عدم وجود `Initialization Code` كمتطلب إنشاء حساب نهائي.
- تحقق من أن الباحث مستقل عن Employee.
- تحقق من أن المدير إشرافي/اطلاعي فقط.
- تحقق من وجود المنتسبين السابقين وإتاحة الكتب للمنتسبين وقواعد الزمنيات.

## النتيجة المطلوبة
أعد تقريرًا فقط يذكر:
- الملفات المعدلة/المضافة.
- نتيجة `git diff --check`.
- أي تعارض غير متوقع وجده Cline.
- ثم توقف.
